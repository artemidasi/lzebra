import * as React from 'react';

import Sections from '@/sections';
import Layout from 'app/layout';

const App: React.FC = () => {
  return (
    <Layout>
      <Sections />
    </Layout>
  );
};

export default App;
